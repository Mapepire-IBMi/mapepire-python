name: mapepire-dev
channels:
  - defaults
  - conda-forge
dependencies:
  - python>=3.8
  - pytest
  - pip
  - python-dotenv
  - pytest-env
  - pytest-asyncio
  - isort
  - black
  - ruff
  - mypy
  - pip:
    - dataclasses-json>=0.6.4
    - websockets>=14.0
    - pyee
    - websockets
    - pep249abc
